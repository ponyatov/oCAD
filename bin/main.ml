(* {%html: <img src="svg.svg"> %} *)

(* {ul
      {li
        {ul
          {li lists}
          {li can be nested}
          {li and can include references}
          {li {!foo}}}}} *)

let () = print_endline "Hello, World!"
